<%- include('../partials/header', { title: 'Money Keeper - Cài đặt' , additionalCSS: [''] }) %>

  <div class="app-container">
    <%- include('../partials/sidebar', { currentPage: currentPage }) %>
      <main class="main-content">
        <header class="top-bar">
          <h1>💼 Cài Đặt Money Keeper</h1>
          <div class="user-info">
            <span id="user-name">User</span>
            <button class="btn-logout">Đăng xuất</button>
          </div>
        </header>
        <div class="content">
          <div class="card">
            <div class="card-header">
              <h2>🔐 Thông Tin Đăng Nhập Money Keeper</h2>
            </div>
            <div class="card-body">
              <p class="info-text">Nhập thông tin đăng nhập Money Keeper để tích hợp và đồng bộ dữ liệu tài chính của bạn.</p>

              <div id="configStatus" class="config-status" style="display: none;">
                <!-- Will show current configuration status -->
              </div>

              <form id="moneyKeeperConfigForm">
                <div class="form-group">
                  <label for="username">Username / Email *</label>
                  <input type="text" id="username" name="username" class="form-control" required
                    placeholder="Nhập username hoặc email Money Keeper">
                </div>

                <div class="form-group">
                  <label for="password">Password *</label>
                  <input type="password" id="password" name="password" class="form-control" required
                    placeholder="Nhập mật khẩu Money Keeper">
                </div>

                <div class="form-actions">
                  <button type="button" id="btnCheckAccount" class="btn btn-secondary">
                    <span class="btn-text">🔍 Kiểm Tra Tài Khoản</span>
                    <span class="btn-loading" style="display: none;">⏳ Đang kiểm tra...</span>
                  </button>
                  <button type="submit" id="btnSaveConfig" class="btn btn-primary" style="display: none;">
                    <span class="btn-text">💾 Lưu Cấu Hình</span>
                    <span class="btn-loading" style="display: none;">⏳ Đang lưu...</span>
                  </button>
                </div>
              </form>

              <div id="validationMessage" class="message" style="display: none;"></div>
            </div>
          </div>

          <!-- Wallet List Section (shown after successful validation) -->
          <div id="walletSection" class="card" style="display: none;">
            <div class="card-header">
              <h2>💰 Danh Sách Ví Money Keeper</h2>
            </div>
            <div class="card-body">
              <p class="info-text">Các ví được tìm thấy trong tài khoản Money Keeper của bạn:</p>
              <div id="walletList" class="wallet-list">
                <!-- Wallets will be populated here -->
              </div>
            </div>
          </div>
        </div>
      </main>
  </div>

  <style>
    .config-status {
      padding: 15px;
      margin-bottom: 20px;
      border-radius: 8px;
      background-color: #e8f5e9;
      border-left: 4px solid #4caf50;
    }

    .config-status.warning {
      background-color: #fff3e0;
      border-left-color: #ff9800;
    }

    .info-text {
      color: #666;
      margin-bottom: 20px;
      line-height: 1.6;
    }

    .form-group {
      margin-bottom: 20px;
    }

    .form-group label {
      display: block;
      margin-bottom: 8px;
      font-weight: 600;
      color: #333;
    }

    .form-control {
      width: 100%;
      padding: 12px;
      border: 1px solid #ddd;
      border-radius: 6px;
      font-size: 14px;
      transition: border-color 0.3s;
    }

    .form-control:focus {
      outline: none;
      border-color: #007bff;
      box-shadow: 0 0 0 3px rgba(0, 123, 255, 0.1);
    }

    .form-actions {
      display: flex;
      gap: 12px;
      margin-top: 24px;
    }

    .btn {
      padding: 12px 24px;
      border: none;
      border-radius: 6px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
    }

    .btn-primary {
      background-color: #007bff;
      color: white;
    }

    .btn-primary:hover {
      background-color: #0056b3;
    }

    .btn-secondary {
      background-color: #6c757d;
      color: white;
    }

    .btn-secondary:hover {
      background-color: #545b62;
    }

    .btn:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }

    .btn-loading {
      display: none;
    }

    .message {
      padding: 12px;
      margin-top: 16px;
      border-radius: 6px;
      font-size: 14px;
    }

    .message.success {
      background-color: #d4edda;
      color: #155724;
      border: 1px solid #c3e6cb;
    }

    .message.error {
      background-color: #f8d7da;
      color: #721c24;
      border: 1px solid #f5c6cb;
    }

    .wallet-list {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
      gap: 16px;
      margin-top: 16px;
    }

    .wallet-card {
      border: 1px solid #e0e0e0;
      border-radius: 8px;
      padding: 16px;
      background-color: #fafafa;
      transition: all 0.3s;
    }

    .wallet-card:hover {
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      border-color: #007bff;
    }

    .wallet-card.inactive {
      opacity: 0.6;
      background-color: #f5f5f5;
    }

    .wallet-header {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 12px;
    }

    .wallet-logo {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      object-fit: cover;
    }

    .wallet-logo-placeholder {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background-color: #007bff;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: bold;
      font-size: 18px;
    }

    .wallet-name {
      font-weight: 600;
      font-size: 16px;
      color: #333;
      flex: 1;
    }

    .wallet-type-badge {
      padding: 4px 8px;
      border-radius: 4px;
      font-size: 11px;
      font-weight: 600;
      text-transform: uppercase;
    }

    .wallet-type-1 {
      background-color: #e3f2fd;
      color: #1976d2;
    }

    .wallet-type-2 {
      background-color: #fff3e0;
      color: #f57c00;
    }

    .wallet-type-3 {
      background-color: #f3e5f5;
      color: #7b1fa2;
    }

    .wallet-info {
      margin-top: 8px;
      font-size: 13px;
      color: #666;
    }

    .wallet-info div {
      margin-bottom: 4px;
    }

    .wallet-amount {
      font-size: 18px;
      font-weight: 700;
      color: #2e7d32;
      margin-top: 12px;
    }

    .wallet-amount.negative {
      color: #c62828;
    }

    .no-wallets {
      text-align: center;
      padding: 40px;
      color: #999;
    }
  </style>

  <%- include('../partials/footer', { additionalJS: [ '/js/money-keeper-setting.js' ] }) %>
