<%- include('partials/header', { title: 'Dashboard' , additionalCSS: ['/css/dashboard.css'] , currentPage: currentPage,
  pageTitle: 'Dashboard' , pageDescription: '' }) %>

  <!-- Summary Cards -->
  <div class="row g-3 mb-4">
    <div class="col-lg-3 col-md-6 col-sm-6">
      <div class="summary-card">
        <div class="card-icon">💰</div>
        <div class="card-content">
          <h3>Tổng thu nhập</h3>
          <p class="amount" id="total-income">0 VND</p>
          <span class="period">Tháng này</span>
        </div>
      </div>
    </div>
    <div class="col-lg-3 col-md-6 col-sm-6">
      <div class="summary-card">
        <div class="card-icon">💸</div>
        <div class="card-content">
          <h3>Tổng chi tiêu</h3>
          <p class="amount" id="total-expense">0 VND</p>
          <span class="period">Tháng này</span>
        </div>
      </div>
    </div>
    <div class="col-lg-3 col-md-6 col-sm-6">
      <div class="summary-card">
        <div class="card-icon">🏠</div>
        <div class="card-content">
          <h3>Tiền thuê nhà</h3>
          <p class="amount" id="total-rent">0 VND</p>
          <span class="period">Tháng này</span>
        </div>
      </div>
    </div>
    <div class="col-lg-3 col-md-6 col-sm-6">
      <div class="summary-card">
        <div class="card-icon">💵</div>
        <div class="card-content">
          <h3>Tiết kiệm</h3>
          <p class="amount" id="total-savings">0 VND</p>
          <span class="period">Tổng cộng</span>
        </div>
      </div>
    </div>
  </div>

  <!-- Chart Controls -->
  <div class="chart-controls mb-3">
    <button class="btn btn-primary btn-sm" id="refresh-charts" title="Làm mới dữ liệu">
      <i class="bi bi-arrow-clockwise"></i> Làm mới
    </button>
  </div>

  <!-- Quick Stats -->
  <div class="quick-stats mb-4">
    <div class="stat-item">
      <span class="stat-label">Số giao dịch tháng này</span>
      <span class="stat-value" id="transaction-count">-</span>
    </div>
    <div class="stat-item">
      <span class="stat-label">Chi tiêu trung bình/ngày</span>
      <span class="stat-value" id="avg-daily-expense">-</span>
    </div>
    <div class="stat-item">
      <span class="stat-label">Danh mục chi nhiều nhất</span>
      <span class="stat-value" id="top-category">-</span>
    </div>
  </div>

  <!-- Charts -->
  <div class="charts-grid">
    <div class="chart-card">
      <h3>Chi tiêu theo danh mục</h3>
      <div id="expense-chart-container" style="position: relative; height: 300px;">
        <canvas id="expense-chart"></canvas>
      </div>
    </div>
    <div class="chart-card">
      <h3>Thu nhập vs Chi tiêu</h3>
      <div id="income-expense-chart-container" style="position: relative; height: 300px;">
        <canvas id="income-expense-chart"></canvas>
      </div>
    </div>
  </div>

  <!-- Recent Activities -->
  <div class="recent-activities">
    <h3>Hoạt động gần đây</h3>
    <div id="activities-list" class="activities-list">
      <p class="loading">Đang tải dữ liệu...</p>
    </div>
  </div>

  <%- include('partials/footer', { additionalJS: [ '/js/dashboard.js' ] }) %>
